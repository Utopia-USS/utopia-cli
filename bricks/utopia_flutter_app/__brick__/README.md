# {{project_name}}

{{description}}

Built on [utopia_arch](https://pub.dev/packages/utopia_arch) +
[utopia_hooks](https://pub.dev/packages/utopia_hooks).

## Getting started

```bash
flutter pub get
flutter run
```

A sample feature lives at `lib/screen/counter/` — open it to see the
Screen/State/View pattern in three small files.

## Project layout

```
lib/
├── app/        # app shell — routing, theming, top-level providers
├── common/     # shared widgets and constants
├── screen/     # one folder per feature (Screen + State + View)
└── util/       # helpers and extensions
```

## Next steps

1. Update Google Sheets `docId` / `sheetId` in
   `lib/app/app_localizations.dart` (or remove localization if you don't
   need it yet).
2. Run code generation:
   ```bash
   dart run build_runner build --delete-conflicting-outputs
   ```
3. Read [`DEVELOPMENT.md`](DEVELOPMENT.md) for the Screen/State/View
   pattern in detail.
4. (Optional) Open Claude Code and run `/utopia-hooks` to scaffold your
   first real screen — see [`.claude/README.md`](.claude/README.md).

## Building

```bash
flutter build apk     --build-name=1.0.0 --build-number=1
flutter build appbundle --build-name=1.0.0 --build-number=1
flutter build ipa     --build-name=1.0.0 --build-number=1
```

---
Generated by [Utopia CLI](https://github.com/Utopia-USS/utopia-cli) 🦄 · Powered by [utopia_hooks](https://pub.dev/packages/utopia_hooks) + [utopia_arch](https://pub.dev/packages/utopia_arch)
