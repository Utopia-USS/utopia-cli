name: utopia_arch_cli
description: CLI tool for creating Flutter projects based on utopia_arch
version: 0.1.0-dev.6
homepage: https://github.com/Utopia-USS/utopia_arch_cli

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.4.0
  mason: ^0.1.0-dev.50
  path: ^1.8.3

executables:
  utopia_arch_cli:

dev_dependencies:
  test: ^1.24.0
